<Window x:Class="DependencyProperty.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:local="clr-namespace:DependencyProperty"
        
        Title="MainWindow" Height="350" Width="525">
    
    <Window.Resources>
        <local:User Name="user1" Age="11"  x:Key="user1Key" />
    </Window.Resources>

    <Grid x:Name="grid1" DataContext="{StaticResource user1Key}">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="Name" />
        <TextBlock Text="{Binding Name}" Grid.Column="1"  />
        <TextBlock Text="Age" Grid.Row="1" />
        <TextBox Text="{Binding Age, Mode=TwoWay}" Grid.Column="1" Grid.Row="1"  />
        <Button Content="Check" Grid.Row="2" Grid.Column="2" Click="Button_Click" />
    </Grid>
</Window>
